# Requested improvements

- [x] Refresh the sidebar automatically when files or folders change (up to 3 seconds).
- [x] Highlight newly added and modified Git files with distinct colors.
- [x] Render fenced `mermaid` diagrams inside Markdown files.
- [x] Render standalone `.mmd` and `.mermaid` files.
- [x] Build and test the frontend and Tauri backend.
